<script>
	var rootURL = "${pageContext.request.contextPath}";
	var fileSeparator = "${fileSeparator}${fileSeparator}";
	var parentMenu = rootURL + "/${parentMenu}";

	var AppConstants = ${AppConstantsJson.json};
	var PageMessages = ${PageMessagesJson.json};
</script>